# 키 점검 로그

커밋 내용에 인증키가 섞이지 않았는지 커밋 직전에 열어 확인하고 한 줄씩 남긴다.
확인 방법: `git diff --cached` 를 열어 `OC=` · `serviceKey` · `api key` · `secret` · `token` 을 찾고,
PDF·PNG 같은 이진파일은 `grep -a` 로 따로 본다.

- 2026-09-21 · 스테이징 22개 파일(텍스트 diff + PDF·PNG 이진 검사)을 열어 확인 — **인증키 없음**. 걸린 것은 GitHub 저장소 주소뿐이고, `scripts/fetch_law_articles.ps1` 의 사용 예시에 있던 실제 OC 값은 `<본인OC>` 로 바꿨으며, 응답 상세링크에 OC가 들어 있는 `out/` 은 `.gitignore` 로 제외했다.

- 2026-09-21 · 제출물 4개 파일(과제 HTML·PDF, 화면·커밋 갈무리 PNG)을 `git diff --cached` 와 `grep -a` 로 열어 확인 — **인증키 없음**. 갈무리 이미지에 주소창·터미널이 찍히지 않았는지도 함께 봤다.

- 2026-09-21 · 카드·CSV에 법령ID·조문번호·시행일·조회일을 명시하도록 고친 뒤 다시 확인 — **인증키 없음**. 새로 생긴 값은 모두 공개 식별자(법령ID·MST)뿐이다.

## 남는 주의점

- 국가법령정보 OC는 가입 이메일의 `@` 앞부분이라 GitHub 사용자명에서 유추될 수 있다. OC만으로는 열리지 않고 **마이페이지에 등록한 IP에서 호출할 때만** 응답이 오지만, OC를 코드에 적지 않는 원칙은 그대로 지킨다.
- 키는 환경변수 `LAW_OC` 에만 두고, 스크립트에는 이름만 적는다.
